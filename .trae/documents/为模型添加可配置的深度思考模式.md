1. **修改OllamaConfig类**：在`config.py`中添加`deep_thought_mode`布尔类型参数，用于控制深度思考模式
2. **更新环境变量配置**：在`.env.example`和`.env`文件中添加`OLLAMA_DEEP_THOUGHT_MODE`环境变量
3. **修改EntityExtractor类**：

   * 更新初始化方法，接收`deep_thought_mode`参数

   * 修改`create_extraction_prompt`方法，根据`deep_thought_mode`生成不同的提示模板

   * 深度思考模式下，提示模板将包含更详细的思考步骤和分析要求
4. **更新main.py和graph\_builder.py**：确保深度思考模式参数正确传递给EntityExtractor实例

