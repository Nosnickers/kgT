## 实施计划：增强口腔科知识图谱提取系统

### 第一阶段：修复提示语偏见问题（最紧急）

**目标**：确保LLM能正确提取所有三个患者的信息，消除示例偏见

1. **修改** **`entity_extractor.py`** **中的示例部分**：

   * 将第179-185行的具体示例（"林悦"、"MK20231026018"等）改为通用占位符

   * 使用业界标准的方括号占位符格式：`[患者姓名]`、`[病历号]`、`[年龄]`等

   * 添加明确说明："以下示例仅展示提取格式，实际值请从文本中提取"

2. **增强提示语通用性指令**：

   * 在EXTRACTION GUIDELINES部分添加："提取所有患者信息，不限于第一个患者"

   * 强调："文本可能包含多个患者，请为每个患者提取相应的实体"

### 第二阶段：增强关系类型体系

**目标**：添加口腔科材料相关关系，丰富知识图谱连接性

1. **扩展** **`RELATIONSHIP_TYPES`** **列表**：

   * 添加：`PROCEDURE_USES_MATERIAL`（治疗使用材料）

   * 添加：`MATERIAL_HAS_COMPONENT`（材料包含成分）

   * 添加：`PROCEDURE_PERFORMED_BY`（治疗由医生执行）

   * 添加：`PROCEDURE_AIMS_TO_ACHIEVE`（治疗旨在达成目标）

   * 添加：`PATIENT_HAS_OCCUPATION`（患者有职业）

   * 添加：`PATIENT_IS_FIRST_VISIT`（患者首次就诊）

2. **更新提示语中的关系类型描述**：

   * 在"RELATIONSHIP TYPES"部分详细描述每种新关系类型的用途

   * 提供示例：如"'使用材料：不含氟抛光膏' → PROCEDURE\_USES\_MATERIAL关系"

### 第三阶段：改进信息传递机制

**目标**：充分利用chunk的title信息，提高提取准确性

1. **修改** **`graph_builder.py`** **的信息传递**：

   * 在 `_extract_with_retry` 和 `_extract_with_logging` 方法中组合title和content

   * 格式：`combined_text = f"标题：{chunk.metadata.get('title', '')}\n内容：{chunk.content}"`

   * 确保日志记录同时显示title和content

2. **优化deep thought模式处理**：

   * 取消注释 `entity_extractor.py` 中的stop参数设置（第109-111行）

   * 实现条件性设置：当 `deep_thought_mode=False` 时，设置 `stop=["<think>", "</think>"]`

   * 在提示语中保留"NO THINKING OUTPUT"指令

### 第四阶段：测试验证

**目标**：确保所有修改有效且不破坏现有功能

1. **创建测试验证脚本**：

   * 测试多患者提取：验证三个患者（林悦、王毛毛、张大大）都能被正确识别

   * 测试新关系类型：验证材料相关关系能被正确提取

   * 测试title信息利用：验证从title中提取的患者信息

   * 测试deep thought模式：验证不再输出思考过程

2. **运行完整流程验证**：

   * 执行知识图谱构建流程，检查所有患者信息是否被提取

   * 验证图数据库包含三个患者的实体和关系

   * 检查新关系类型是否正确存储在Neo4j中

### 实施顺序与验证点

1. **先修复提示语偏见** → 验证：三个患者都能被识别
2. **然后增强关系类型** → 验证：材料相关关系被提取
3. **接着改进信息传递** → 验证：title信息被利用
4. **最后优化配置** → 验证：deep thought模式正常工作

### 风险缓解措施

* **风险**：修改示例可能影响提取模式

  * **缓解**：保持格式不变，只替换具体值为占位符，分步测试

* **风险**：stop参数可能导致空响应

  * **缓解**：先测试小范围，如有问题调整为 `stop=["<think>"]` 或移除

* **风险**：新关系类型可能混淆LLM

  * **缓解**：在提示语中提供清晰定义和示例，限制使用条件

### 预期成果

1. **消除患者识别偏见**：系统正确提取所有三个患者信息
2. **丰富关系类型**：口腔科材料相关关系被完整提取
3. **提高信息利用率**：title中的关键信息被有效提取
4. **控制输出格式**：deep thought模式被正确禁用，只输出JSON

