## 计划：创建LLM输入输出日志测试脚本

### 当前问题分析
用户发现提取的实体与关系不存在于原始Dulce.json数据中，需要分析chunk_id 22的具体处理过程来排查幻觉原因。

### 解决方案
创建一个独立的测试脚本，专门用于记录chunk_id 22的LLM完整交互过程：

1. **创建测试脚本** `test_chunk_22_logging.py`：
   - 读取CSV文件获取chunk_id 22的内容
   - 模拟完整的LLM提取流程
   - 详细记录所有输入输出到日志文件
   - 包含原始提示词、LLM响应、JSON解析结果

2. **日志记录内容**：
   - 完整的系统提示词（包含实体类型和关系类型定义）
   - 用户输入文本（chunk_id 22的内容）
   - LLM原始响应（包含思考过程标签）
   - JSON清理后的结果
   - 验证和过滤过程
   - 最终提取的实体和关系

3. **不影响原始功能**：
   - 创建独立的测试脚本，不修改现有代码
   - 使用相同配置但独立的日志文件
   - 提供详细的调试信息用于分析幻觉原因

4. **使用方法**：
   ```bash
   python test_chunk_22_logging.py --chunk-id 22 --config .env
   ```

### 预期输出
- 详细的LLM交互日志文件
- chunk_id 22内容的完整处理流程
- 便于分析为什么提取的实体不存在于原始数据中