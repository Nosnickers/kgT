## 问题分析
日志显示 LLM 看到的文本是 `<text>` 占位符而不是实际的病历内容。这表明提示词模板中的 `{text}` 占位符没有被正确替换。

**根本原因**：在 `graph_builder.py` 的 `_extract_with_logging` 方法中，`prompt.format_messages(text=chunk.content)` 可能没有正确替换 `{text}` 占位符。虽然代码逻辑看起来正确，但实际执行时格式化可能失败。

## 修复计划

### 阶段一：诊断和调试
1. **添加详细的调试日志**：
   - 在 `_extract_with_logging` 方法中添加日志，记录 `formatted_prompt` 的内容，确认 `{text}` 是否被正确替换。
   - 记录 `prompt.messages[1].content` 的原始值，检查模板是否正确。
   - 记录 `chunk.content` 的值，确保文本内容不为空。

2. **检查格式化结果**：
   - 添加临时代码来检查 `formatted_prompt` 中每个消息的内容。
   - 验证 `ChatPromptTemplate` 是否按预期工作。

### 阶段二：修复提示词格式化
1. **直接字符串格式化**（备选方案）：
   - 如果 `ChatPromptTemplate` 的格式化有问题，可以改为直接使用字符串格式化：
     ```python
     user_prompt_content = human_prompt.format(text=chunk.content)
     formatted_prompt = [
         SystemMessage(content=base_prompt),
         HumanMessage(content=user_prompt_content)
     ]
     ```

2. **检查占位符语法**：
   - 确保 `{text}` 是有效的 Python 字符串格式化占位符。
   - 验证没有转义或编码问题。

### 阶段三：验证修复
1. **运行测试**：
   - 运行 `test_end_to_end.py` 验证提示词格式化是否正常工作。
   - 检查日志中 LLM 是否能看到实际的病历内容。

2. **运行完整流程**：
   - 执行 `python main.py --build --data-file data/oralChunks.md --enable-llm-logging`
   - 验证知识图谱构建流程中所有 chunk 的提示词都能正确格式化。

### 阶段四：预防措施
1. **添加输入验证**：
   - 在格式化前验证 `chunk.content` 不为空且长度合理。
   - 添加错误处理，当格式化失败时提供明确的错误信息。

2. **完善日志记录**：
   - 在关键步骤添加更详细的日志，便于未来调试。

## 预期结果
- LLM 接收到的提示词中包含实际的病历内容，而不是 `<text>` 占位符。
- 实体和关系提取能够基于实际文本进行，而不是返回空数组。
- 知识图谱构建流程恢复正常工作。