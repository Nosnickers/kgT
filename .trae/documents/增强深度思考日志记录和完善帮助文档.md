## 实施计划

### 1. 增强 GraphBuilder 的日志记录功能
修改 `src/graph_builder.py` 中的 `_extract_with_logging` 方法：
- 添加对系统提示词和用户提示词的记录
- 添加对LLM原始响应的记录（包括深度思考过程）
- 添加对清理后JSON响应的记录
- 确保所有LLM交互过程都被完整记录

### 2. 完善 main.py 的帮助文档
修改 `main.py`：
- 为 `--enable-llm-logging` 参数添加详细的帮助说明
- 添加对深度思考模式的说明
- 更新整体帮助文档，使其更清晰易懂

### 3. 测试验证
- 运行 `main.py --build --enable-llm-logging` 验证日志记录功能
- 确认深度思考模式的输出被正确记录到日志文件中